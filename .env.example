# Public browser-safe Supabase values only.
# Copy this file to .env.local and add the values there.
# NEVER commit service_role keys, database passwords, access tokens, or real user data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
